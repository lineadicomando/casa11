/**
 * La resa per la lettura: il tema calcolato più le istruzioni per leggerlo.
 *
 * Un pacchetto e non un modulo di `apps/web` perché lo vogliono tutte le
 * superfici, e il ragionamento per esteso sta in `lettura.ts`.
 */

export * from './lettura.js';
export * from './jyotisha.js';
export * from './tropicale.js';
